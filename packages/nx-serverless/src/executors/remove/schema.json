{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Remove executor",
  "description": "",
  "type": "object",
  "properties": {
    "noError": {
      "type": "boolean",
      "description": "If true it will not throw error.",
      "default": false
    },
    "showHelp": {
      "type": "boolean",
      "description": "Show sls help for that command."
    }
  },
  "required": []
}
