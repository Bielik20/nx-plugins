{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Serve executor",
  "description": "",
  "type": "object",
  "properties": {
    "showHelp": {
      "type": "boolean",
      "description": "Show sls help for that command."
    },
    "out": {
      "type": "string",
      "description": "Location where tsc artifacts are stored."
    }
  },
  "required": []
}
